namespace Knossos.NET.Models
{
    /*
        Only for legacy mod support, it should only be used if there is no mod.json
        Read-only, used to generate a working Mod model in runtime when loading the mod.
    */
    public class ModIni
    {
    }
}
